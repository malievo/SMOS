"""
config.py — загрузка общих настроек Wake+Req (SMOS FWL) из config.json.

Идея: все настройки, которые раньше были разбросаны константами по
req.py и wake.py, теперь лежат в одном файле config.json рядом со
скриптами. Чтобы поменять поведение — не нужно лезть в код, достаточно
поправить config.json и перезапустить скрипты (конфиг читается заново
при каждом запуске, "на лету" не подхватывается).

Если config.json отсутствует, не найден или содержит битый JSON — оба
скрипта не падают, а просто работают на значениях по умолчанию (см.
DEFAULTS ниже) и печатают предупреждение в консоль.

Если в config.json заполнены не все поля — недостающие берутся из
DEFAULTS (рекурсивное слияние), то есть файл можно редактировать
частично, не переписывая целиком.

Использование в req.py / wake.py:
    import config
    CFG = config.load(SCRIPT_DIR)
    CFG["audio"]["sample_rate"]
    ...
"""

import copy
import json
from pathlib import Path

CONFIG_FILENAME = "config.json"

# Значения по умолчанию — используются целиком, если config.json нет,
# и как "заглушка" для отдельных полей, если их нет в config.json.
DEFAULTS = {
    # Язык распознавания речи (код языка Google STT).
    "language": "ru-RU",
    # Пауза между проверками flags/utterance.wav в req.py, сек. Чем
    # меньше — тем быстрее req.py реагирует на новую запись, но тем
    # больше грузит CPU пустыми проверками.
    "check_interval_sec": 0.2,

    "audio": {
        # Частота дискретизации микрофона, Гц. ДОЛЖНА быть одинаковой у
        # wake.py и req.py — wake.py пишет WAV, req.py его читает этими
        # же параметрами.
        "sample_rate": 16000,
        # Битность сэмпла. Сейчас поддерживается только 16 (стандарт
        # для распознавания речи). Меняй, только если точно знаешь зачем.
        "bit_depth": 16,
        # Число каналов микрофона. 1 = моно (рекомендуется).
        "channels": 1,
        # Размер блока чтения в wake.py, сэмплов. openWakeWord ожидает
        # блоки ~80мс — при sample_rate=16000 это 1280.
        "chunk_size": 1280,
    },

    "wake_word": {
        # Путь к своей обученной модели активационного слова (.onnx
        # или .tflite), относительно папки со скриптами, либо null —
        # тогда загружаются ВСЕ встроенные предобученные модели
        # openWakeWord ("hey jarvis", "alexa" и др.) для теста.
        "model_path": None,
        # Порог уверенности детекции активационного слова, 0.0-1.0.
        # Подбирается на практике: выше — меньше ложных срабатываний,
        # но модель может не расслышать слово; ниже — наоборот.
        "detection_threshold": 0.5,
        # Дебаунс: минимальный интервал (сек) между двумя срабатываниями
        # детекции активационного слова. Само произнесённое слово обычно
        # длится дольше одного звукового чанка (~80мс), и модель может
        # держать score выше порога сразу в нескольких соседних чанках —
        # без дебаунса это давало ложные повторные срабатывания НА ОДНО
        # и то же слово. Не должно быть короче типичной длительности
        # произнесения слова.
        "detection_cooldown_sec": 2.0,
        # Сколько секунд аудио держим "про запас" в кольцевом буфере
        # wake.py — должно с запасом перекрывать задержку детекции
        # модели + время, за которое человек успевает произнести
        # следующее слово тем же выдохом. Если в debug_audio всё ещё
        # обрезано начало фразы — увеличь.
        "prebuffer_seconds": 1.5,
        # Сколько секунд после успешного распознавания (flags/activity.flag)
        # разрешаем начинать запись СЛЕДУЮЩЕЙ фразы без повторного
        # произнесения активационного слова — чтобы не заставлять
        # заново говорить "Алекса" посреди диалога.
        "continuation_window_sec": 30,
    },

    "recording": {
        # Фиксированный порог громкости (RMS сэмпла), выше которого чанк
        # считается речью, а не тишиной. Подбирается под свой
        # микрофон/уровень шума в комнате опытным путём. Используется
        # wake.py и для определения конца фразы, и (в окне продолжения
        # диалога) для определения, что кто-то снова заговорил.
        "energy_threshold": 300,
        # Сколько секунд тишины подряд считать концом фразы.
        # Больше — надёжнее ловит конец фразы (меньше риск обрезать
        # человека на полуслове), но медленнее отвечает.
        "pause_threshold_sec": 1.3,
        # Предохранитель: максимальная длина одной записываемой фразы,
        # секунд. Если тишина в комнате почему-то никогда не наступает
        # (шумно, микрофон "залип" и т.п.) — запись всё равно
        # принудительно завершится и уйдёт на распознавание, а не будет
        # копиться бесконечно.
        "max_utterance_seconds": 15,
    },

    # Имена файлов и папок, все относительно папки со скриптами.
    # Трогать без необходимости не стоит — это скорее для случая,
    # когда потребуется развести пути (например, при переносе flags
    # в общую папку модуля Ear, как задумано на будущее).
    "paths": {
        "flags_dir": "flags",
        "utterance_file": "utterance.wav",
        "activity_file": "activity.flag",
        "output_dir": "output",
        "output_file": "recognized.json",
        "debug_audio_dir": "debug_audio",
        "debug_audio_file": "last_recording.wav",
    },
}


def _deep_merge(base: dict, override: dict) -> dict:
    """Рекурсивно накладывает override поверх base. Ключи, отсутствующие
    в override, остаются от base — то есть config.json можно заполнять
    частично."""
    result = copy.deepcopy(base)
    for key, value in override.items():
        if isinstance(value, dict) and isinstance(result.get(key), dict):
            result[key] = _deep_merge(result[key], value)
        else:
            result[key] = value
    return result


def load(base_dir: Path) -> dict:
    """Загружает config.json из папки base_dir (обычно — папка самого
    скрипта, SCRIPT_DIR) и накладывает его поверх DEFAULTS.

    Не бросает исключений наружу: если файла нет или он битый —
    печатает предупреждение и возвращает DEFAULTS, чтобы опечатка в
    конфиге не роняла весь скрипт."""
    config_file = Path(base_dir) / CONFIG_FILENAME

    if not config_file.exists():
        print(f"[config] {CONFIG_FILENAME} не найден рядом со скриптом — использую значения по умолчанию.")
        return copy.deepcopy(DEFAULTS)

    try:
        with open(config_file, "r", encoding="utf-8") as f:
            user_config = json.load(f)
    except (json.JSONDecodeError, OSError) as e:
        print(f"[config] Не удалось прочитать {CONFIG_FILENAME} ({e}) — использую значения по умолчанию.")
        return copy.deepcopy(DEFAULTS)

    if not isinstance(user_config, dict):
        print(f"[config] {CONFIG_FILENAME} должен содержать JSON-объект — использую значения по умолчанию.")
        return copy.deepcopy(DEFAULTS)

    return _deep_merge(DEFAULTS, user_config)
